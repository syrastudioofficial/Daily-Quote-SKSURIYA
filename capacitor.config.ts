import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dailydoseofjoy.app',
  appName: 'Daily Dose of Joy',
  webDir: 'dist'
};

export default config;
